/**
 * إعدادات مركزية للكلينت — مصدر واحد للحقيقة
 * ─────────────────────────────────────────────────────────
 * كان الرابط متكرر في 3 ملفات بأشكال مختلفة (واحد فيهم بشرطة
 * زيادة في الآخر بتطلع //api) — دلوقتي كله من هنا.
 */

// رابط السيرفر بدون شرطة مائلة في الآخر — مهما كانت قيمة الـ env
export const SERVER_BASE_URL = (
  import.meta.env.VITE_API_URL || "https://branch-visit-tracker.up.railway.app"
).replace(/\/+$/, "");

// حدود مزامنة الأوفلاين مطابقة لحدود الـ zod على السيرفر
export const MAX_LOCATIONS_PER_SYNC = 1000; // السيرفر يقبل 2000 — بنبعت أقل للأمان
export const MAX_VISITS_PER_SYNC = 40;      // السيرفر يقبل 50
